export const globalProps = {
	navigatorOn : true,
	letterSize : 8,
	letterHeight: 9.15,
	maxRowCount : 86,
	maxRowWidth: 64,

	scale : function (canvas) {
		var width = canvas.width
		var height = canvas.height

	}

}